import React from 'react';

/**
 * Represents Chatbot Header component.
 * @function
 * @param {object} props - props passed from parent component
 */
const Header = () => (
  <header role="banner" className="header">
    <h2>Chatbot - Need help? </h2>
  </header>
);

export default Header;
